import "package:flutter/material.dart";

class PublishClassScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Publish Screen"),
      ),
    );
  }
}
